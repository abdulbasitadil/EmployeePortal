import React from 'react';
import './Position.css';
import PositionTable from './PositionTable';

const Position = () => {
  return (
    <React.Fragment>
      <PositionTable />
    </React.Fragment>
  );
};

export default Position;
